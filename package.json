{
  "name": "back-to-the-future-fantom",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ 0xszeth/back-to-the-future-fantom",
    "create-local": "graph create --node http://localhost:8020/ 0xszeth/back-to-the-future-fantom",
    "remove-local": "graph remove --node http://localhost:8020/ 0xszeth/back-to-the-future-fantom",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 0xszeth/back-to-the-future-fantom"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.2",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
